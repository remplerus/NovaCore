plugins {
    id 'java-library'
    id 'eclipse'
    id 'idea'
    id 'maven-publish'
    id "com.modrinth.minotaur" version "2.+"
    id "com.matthewprenger.cursegradle" version "1.4.0"
    id 'net.neoforged.gradle.userdev' version '7.0.133'
    id 'com.diffplug.spotless' version '6.23.3'
}

if (project.file('../gradletools.gradle').exists()) {
    apply from: '../gradletools.gradle'
} else {
    apply from: 'https://raw.githubusercontent.com/NovaMachina-Mods/MultiWorkspace/1.20/gradletools.gradle'
}

def basicVersion = "${project.mod_version}-build." + ext.getBuildNumber()
version = "${project.minecraft_version}-${basicVersion}" + (basicVersion.endsWith("SNAPSHOT") ?  '' : (project.release_type != 'release' ? "-${project.release_type}" : ''))
group = project.mod_group_id + project.mod_id

java.toolchain.languageVersion = JavaLanguageVersion.of(21)

sourceSetConfig()
repos()
at()
runConfigurations()
jars()
ext.processResources([])

dependencies {
    mc()
}

ext.publish()

if(!System.getenv('CI')) {
    apply plugin: com.diffplug.gradle.spotless.SpotlessPlugin
    spotlessConfig()
}

localGitHook()

cfdeps([], [], [])
modrinthConfig([], [], [])

project.tasks.withType(JavaCompile).configureEach {
    options.encoding = 'UTF-8'
}